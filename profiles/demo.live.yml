# ═════════════════════════════════════════════════
# openDesk SME — Live Demo Profile
# ═════════════════════════════════════════════════
# Public-facing demo with Let's Encrypt HTTPS.
# Minimal footprint: ~2 vCPU, 4 GB RAM.
#
# Use: COMPOSE_FILE="docker-compose.yml:idm/zitadel.yml:opencloud/opencloud.yml:profiles/demo.live.yml"
#   docker compose up -d
#
# Included: Portal, Traefik, PostgreSQL, Redis, Memcached, Zitadel, OpenCloud
# Excluded: PgBouncer, LDAP, Collabora, Stalwart, SOGo
# ═════════════════════════════════════════════════

services:
  # ── PostgreSQL (minimal for demo) ─────────────
  postgres:
    command:
      - "-c"
      - "shared_buffers=128MB"
      - "effective_cache_size=512MB"
      - "work_mem=8MB"
      - "maintenance_work_mem=64MB"
      - "max_connections=50"
      - "random_page_cost=1.1"
      - "effective_io_concurrency=100"
      - "wal_buffers=4MB"
      - "min_wal_size=256MB"
      - "max_wal_size=1GB"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
        reservations:
          cpus: "0.5"
          memory: 256M

  # ── Skip PgBouncer (direct PG connect in demo) ─
  pgbouncer:
    profiles:
      - donotstart

  # ── Redis (minimal, no persistence) ──────────
  redis:
    command:
      - redis-server
      - --save ""
      - --appendonly no
      - --maxmemory 256mb
      - --maxmemory-policy allkeys-lru
      - --loglevel warning
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M
        reservations:
          cpus: "0.1"
          memory: 128M

  # ── Memcached (minimal) ──────────────────────
  memcached:
    command: memcached -m 128 -c 1024 -t 2
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 128M
        reservations:
          cpus: "0.1"
          memory: 64M

  # ── Portal (no raw port, Traefik-only) ────────
  portal:
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
        reservations:
          cpus: "0.1"
          memory: 32M

  # ── LDAP — not used (Zitadel has built-in user store) ──

  # ── OpenCloud (local storage, no S3/MinIO) ───
  opencloud:
    environment:
      OC_ADD_RUN_SERVICES: "notifications"
      STORAGE_USERS_DRIVER: ocis
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 512M

  # ── Collabora — skip (too heavy for demo) ─────
  collabora:
    profiles:
      - donotstart

  # ── Stalwart — skip ──────────────────────────
  stalwart:
    profiles:
      - donotstart

  # ── SOGo — skip ──────────────────────────────
  sogo:
    profiles:
      - donotstart
