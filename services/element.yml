# ── Global defaults ──────────────────────────────────
# Inherited by every service in this file via: <<: *service-defaults
x-service-defaults: &service-defaults
  logging:
    driver: json-file
    options:
      max-size: "50m"
      max-file: "3"
  init: true
  security_opt:
    - no-new-privileges:true

# ═══════════════════════════════════════════════════════════════
# openDesk SME — Element-Web (Matrix Web Client)
# ═══════════════════════════════════════════════════════════════
# Web client for Matrix/Synapse. Optional — users can also use
# native clients (FluffyChat, Element Desktop).
#
# Include via: COMPOSE_FILE=docker-compose.yml:services/element.yml
# Or via profile: docker compose --profile element up -d
#
# Requires: services/synapse.yml (--profile chat)
# ═══════════════════════════════════════════════════════════════

services:
  element-web:
    <<: *service-defaults
    image: elementio/element-web:v1.12.6
    container_name: opendesk-element
    restart: unless-stopped
    depends_on:
      synapse:
        condition: service_healthy
    profiles: ["element"]
    environment:
      ELEMENT_DEFAULT_SERVER_URL: "https://matrix.${OPENDESK_DOMAIN:-opendesk-sme.org}"
    networks:
      - opendesk-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.element.rule=Host(`element.${OPENDESK_DOMAIN:-opendesk-sme.org}`)"
      - "traefik.http.routers.element.tls=true"
      - "traefik.http.routers.element.tls.certresolver=letsencrypt"
      - "traefik.http.routers.element.entrypoints=websecure"
      - "traefik.http.services.element.loadbalancer.server.port=8080"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "-O", "/dev/null", "http://localhost:8080/"]
      interval: 20s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 48M
        reservations:
          cpus: "0.05"
          memory: 8M
