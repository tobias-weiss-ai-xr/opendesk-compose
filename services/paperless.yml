# ── Global defaults ──────────────────────────────────
# Inherited by every service in this file via: <<: *service-defaults
x-service-defaults: &service-defaults
  logging:
    driver: json-file
    options:
      max-size: "50m"
      max-file: "3"
  init: true
  security_opt:
    - no-new-privileges:true

# ═══════════════════════════════════════════════════════════════
# openDesk SME — Paperless-ngx + Gotenberg
# ═══════════════════════════════════════════════════════════════
# Document management with OCR. Gotenberg handles PDF conversion.
# Include via: COMPOSE_FILE=docker-compose.yml:services/paperless.yml
# Or via profile: docker compose --profile paperless up -d
#
# Requires: PostgreSQL (core), Redis (core)
# Optional: Paperless-Tika for enhanced OCR (profile: tika)
# ═══════════════════════════════════════════════════════════════

services:
  paperless-ngx:
    <<: *service-defaults
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: opendesk-paperless
    restart: unless-stopped
    profiles: ["paperless"]
    stop_grace_period: 60s
    tmpfs:
      - /tmp
    environment:
      PAPERLESS_REDIS: redis://redis:6379/2
      PAPERLESS_DBHOST: postgres
      PAPERLESS_DBPORT: "5432"
      PAPERLESS_DBNAME: paperless_db
      PAPERLESS_DBUSER: paperless_user
      PAPERLESS_DBPASS: ${PAPERLESS_DB_PASSWORD:-CHANGEME_paperless}
      PAPERLESS_URL: https://paperless.${OPENDESK_DOMAIN:-opendesk-sme.org}
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY:-CHANGEME_generate_64_chars}
      PAPERLESS_TIME_ZONE: ${TZ:-Europe/Berlin}
      PAPERLESS_OCR_LANGUAGE: deu+eng
      PAPERLESS_OCR_MODE: tesseract
      PAPERLESS_OCR_CLEAN: clean
      PAPERLESS_THREADS: "1"
      PAPERLESS_WEBSERVER_WORKERS: "1"
      PAPERLESS_TASK_WORKERS: "1"
      PAPERLESS_CONSUMER_POLLING: "30"
      PAPERLESS_FILENAME_FORMAT: "{created_year}/{created_month}/{created_day}/{correspondent}/{title}"
      PAPERLESS_CONVERT_MEMORY_LIMIT: 256
      PAPERLESS_GOTENBERG_ENDPOINT: http://paperless-gotenberg:3000
      # Uncomment to enable Tika (requires --profile tika)
      # PAPERLESS_TIKA_ENDPOINT: http://paperless-tika:9998
    volumes:
      - paperless-data:/usr/src/paperless/data
      - paperless-media:/usr/src/paperless/media
      - paperless-export:/usr/src/paperless/export
      - ./services/paperless-consume:/consume:ro
    networks:
      - opendesk-net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      paperless-gotenberg:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.paperless.rule=Host(`paperless.${OPENDESK_DOMAIN:-opendesk-sme.org}`)"
      - "traefik.http.routers.paperless.tls=true"
      - "traefik.http.routers.paperless.tls.certresolver=letsencrypt"
      - "traefik.http.routers.paperless.entrypoints=websecure"
      - "traefik.http.services.paperless.loadbalancer.server.port=8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 384M
        reservations:
          cpus: "0.25"
          memory: 64M

  paperless-gotenberg:
    <<: *service-defaults
    image: gotenberg/gotenberg:8
    container_name: opendesk-gotenberg
    restart: unless-stopped
    profiles: ["paperless"]
    tmpfs:
      - /tmp
    command:
      - "gotenberg"
    networks:
      - opendesk-net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "-O", "/dev/null", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: 96M
        reservations:
          cpus: "0.05"
          memory: 16M

  paperless-tika:
    <<: *service-defaults
    image: ${TIKA_IMAGE:-apache/tika:latest}
    container_name: opendesk-tika
    restart: unless-stopped
    profiles: ["tika"]
    shm_size: 256M
    tmpfs:
      - /tmp
    environment:
      TIKA_MEMORY_LIMIT: 512m
      JAVA_TOOL_OPTIONS: >-
        -XX:MaxRAMPercentage=75.0
        -XX:+UseG1GC
        -XX:MaxGCPauseMillis=200
    networks:
      - opendesk-net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9998/tika || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M

volumes:
  paperless-data:
  paperless-media:
  paperless-export:
