# ── Global defaults ──────────────────────────────────
# Inherited by every service in this file via: <<: *service-defaults
x-service-defaults: &service-defaults
  logging:
    driver: json-file
    options:
      max-size: "50m"
      max-file: "3"
  init: true
  security_opt:
    - no-new-privileges:true

# ── OpenCloud + Collabora overlay ──────────────
# Include via: COMPOSE_FILE=docker-compose.yml:opencloud/opencloud.yml
# Provides file sync/sharing + online office editing.
#
# Storage backend: openCloud's built-in `ocis` (local POSIX filesystem,
# files live in the opencloud-data volume). This avoids the MinIO/S3
# decomposeds3 driver, whose env-var mapping is fragile across versions.
# If you want S3 object storage instead, point OC_STORAGE_USERS_DRIVER at
# `decomposeds3` and supply the OC_STORAGE_USERS_DECOMPOSEDS3_* vars.

services:
  opencloud:
    <<: *service-defaults
    image: opencloudeu/opencloud-rolling:6.0.0
    container_name: opendesk-opencloud
    restart: unless-stopped
    stop_grace_period: 60s
    shm_size: 256M
    tmpfs:
      - /tmp
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - opencloud-config:/etc/opencloud
      - opencloud-config:/var/lib/opencloud
      - opencloud-data:/var/lib/opencloud/data
      - ./opencloud/opencloud-entrypoint.sh:/entrypoint.sh:ro
    environment:
      OC_INSECURE: ${OC_INSECURE:-true}
      # ^ true is correct for Docker Compose — inter-service traffic
      #   stays on the internal network (no TLS). Security boundary is Traefik.
      OC_LOG_LEVEL: ${LOG_LEVEL:-info}
      OC_LOG_PRETTY: ${LOG_PRETTY:-true}
      OC_ADMIN_USERNAME: ${OC_ADMIN_USERNAME:-admin}
      OC_ADMIN_PASSWORD: ${OC_ADMIN_PASSWORD:-CHANGEME_oc_admin}
      OC_DOMAIN: ${OPENCLOUD_DOMAIN:-cloud.opendesk-sme.org}
      OC_URL: https://${OPENCLOUD_DOMAIN:-cloud.opendesk-sme.org}
      OC_IDP_URL: https://${ZITADEL_DOMAIN:-auth.opendesk-sme.org}
      OC_OIDC_ISSUER: https://${ZITADEL_DOMAIN:-auth.opendesk-sme.org}
      OC_OIDC_CLIENT_ID: ${OC_OIDC_CLIENT_ID:-opencloud}
      OC_OIDC_CLIENT_SECRET: ${OC_OIDC_SECRET:-CHANGEME_oidc_secret}
      PROXY_ROLE_ASSIGNMENT_DRIVER: oidc
      # Local POSIX filesystem backend (no MinIO/S3 required).
      OC_STORAGE_USERS_DRIVER: ocis
      OC_ADD_RUN_SERVICES: notifications
      # openCloud 6.0.0 has a startup data race ("concurrent map iteration and
      # map write"/"concurrent map read and map write") that fires on multi-core
      # hosts when many services register concurrently during startup. The only
      # reliable workaround is GOMAXPROCS=1 (fully serialises goroutines). This
      # is single-threaded and slower, but stable; revisit when a patched
      # 6.0.x is available.
      GOMAXPROCS: "1"
      # SMTP (password-reset / share notifications) — relay via Stalwart.
      # NOTE: openCloud 6.0.0 binds these WITHOUT the OC_ prefix
      # (struct tag env:"NOTIFICATIONS_SMTP_SENDER"), so the bare names are
      # required. Startup only validates the sender format; actual delivery
      # needs a matching Stalwart account (configure OC_SMTP_* in your .env).
      NOTIFICATIONS_SMTP_SENDER: ${OC_SMTP_SENDER:-noreply@${OPENDESK_DOMAIN:-opendesk-sme.org}}
      NOTIFICATIONS_SMTP_HOST: ${OC_SMTP_HOST:-stalwart}
      NOTIFICATIONS_SMTP_PORT: ${OC_SMTP_PORT:-587}
      NOTIFICATIONS_SMTP_TLS_INSECURE: ${OC_SMTP_TLS_INSECURE:-true}
      NOTIFICATIONS_SMTP_AUTHENTICATION: ${OC_SMTP_AUTHENTICATION:-login}
      NOTIFICATIONS_SMTP_USERNAME: ${OC_SMTP_USERNAME:-noreply@${OPENDESK_DOMAIN:-opendesk-sme.org}}
      NOTIFICATIONS_SMTP_PASSWORD: ${OC_SMTP_PASSWORD:-CHANGEME_smtp}
      # Performance tuning
      OC_PERMISSIONS_SSH_KEY_AGE_MAX: 0
      OC_STORAGE_USERS_MAX_CONCURRENCY: 10
      OC_HTTP_CACHE_TTL: 3600
      OC_UPLOAD_MAX_SIZE: 10737418240
    user: "0:0"
    # mounted opencloud-entrypoint.sh generates config on first run,
    # then execs the opencloud binary (image ENTRYPOINT is /usr/bin/opencloud)
    entrypoint: ["/entrypoint.sh"]
    command: ["server"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9200/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - opendesk-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.opencloud.rule=Host(`${OPENCLOUD_DOMAIN:-cloud.opendesk-sme.org}`)"
      - "traefik.http.routers.opencloud.tls=true"
      - "traefik.http.routers.opencloud.tls.certresolver=letsencrypt"
      - "traefik.http.routers.opencloud.entrypoints=websecure"
      - "traefik.http.services.opencloud.loadbalancer.server.port=9200"
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 2G

  collabora:
    # No `<<: *service-defaults`: CODE cannot run under no-new-privileges
    # (it Elevates through file capabilities at runtime). Enumerate the other
    # defaults explicitly instead.
    image: ${COLLABORA_IMAGE:-collabora/code:24.04.13.3.1}
    container_name: opendesk-collabora
    restart: unless-stopped
    init: true
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
    shm_size: 512M
    environment:
      aliasgroup1: https://${OPENCLOUD_DOMAIN:-cloud.opendesk-sme.org}:443
      extra_params: >-
        --o:ssl.enable=false
        --o:ssl.termination=true
        --o:num_prespawn_children=4
        --o:max_connections=200
        --o:max_documents=100
      domain: ${OPENDESK_DOMAIN:-opendesk-sme.org}
      DONT_GEN_SSL_CERT: true
      username: ${COLLABORA_USERNAME:-admin}
      password: ${COLLABORA_PASSWORD:-CHANGEME_collabora}
    cap_add:
      - MKNOD
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9980/hosting/discovery"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - opendesk-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.collabora.rule=Host(`${COLLABORA_DOMAIN:-collabora.opendesk-sme.org}`)"
      - "traefik.http.routers.collabora.tls=true"
      - "traefik.http.routers.collabora.tls.certresolver=letsencrypt"
      - "traefik.http.routers.collabora.entrypoints=websecure"
      - "traefik.http.services.collabora.loadbalancer.server.port=9980"
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 1G

volumes:
  opencloud-config:
  opencloud-data:
