# ── Global defaults ──────────────────────────────────
# Inherited by every service in this file via: <<: *service-defaults
x-service-defaults: &service-defaults
  logging:
    driver: json-file
    options:
      max-size: "50m"
      max-file: "3"
  init: true
  security_opt:
    - no-new-privileges:true

# ── Zitadel (Identity & Access Management) ─────────
# Include via: COMPOSE_FILE=docker-compose.yml:idm/zitadel.yml
# Replaces Keycloak. Go-based, no JVM, built-in DB, ~100 MB RAM.
#
# First start: log in at https://auth.<domain>
# Default: zitadel-admin@<domain> / <ZITADEL_ADMIN_PASSWORD>

services:
  zitadel:
    <<: *service-defaults
    image: ${ZITADEL_IMAGE:-ghcr.io/zitadel/zitadel:v3.4.15}
    container_name: opendesk-zitadel
    restart: unless-stopped
    # Fresh installs need `zitadel init` + `zitadel setup` once before `start`
    # (run headlessly by the ansible role / `docker compose run`). The image is
    # distroless — no shell here. TLS is terminated by Traefik (--tlsMode external).
    command: ["start", "--masterkeyFile", "/secrets/masterkey", "--tlsMode", "external"]
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # ── Database ──
      # ── External DB (Postgres 17 in docker-compose.yml) ──
      ZITADEL_DATABASE_POSTGRES_HOST: postgres
      ZITADEL_DATABASE_POSTGRES_PORT: 5432
      ZITADEL_DATABASE_POSTGRES_DATABASE: ${ZITADEL_DB_NAME:-zitadel}
      ZITADEL_DATABASE_POSTGRES_USER_USERNAME: ${POSTGRES_USER:-opendesk}
      ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: ${POSTGRES_PASSWORD}
      ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: ${POSTGRES_USER:-opendesk}
      ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: ${POSTGRES_PASSWORD}
      ZITADEL_DATABASE_POSTGRES_SSL_MODE: disable
      ZITADEL_DATABASE_POSTGRES_USER_SSLMODE: disable
      # ── External domain ──
      ZITADEL_EXTERNALDOMAIN: ${ZITADEL_DOMAIN:-auth.opendesk-sme.org}
      ZITADEL_EXTERNALPORT: 443
      # tlsMode external: Traefik terminates TLS and forwards plain HTTP to
      # zitadel:8080. Keep ExternalSecure=false so 'zitadel ready' probes
      # http://localhost:8080 (not https, which the server does not speak).
      # Public https URLs are derived from Traefik's X-Forwarded-Proto header.
      ZITADEL_EXTERNALSECURE: false
      # ── First admin ──
      ZITADEL_FIRSTINSTANCE_ORGID: opendesk
      ZITADEL_FIRSTINSTANCE_ORGNAME: openDesk
      ZITADEL_FIRSTINSTANCE_USERADMIN_EMAIL: ${ZITADEL_ADMIN_EMAIL:-admin@${ZITADEL_DOMAIN:-auth.opendesk-sme.org}}
      ZITADEL_FIRSTINSTANCE_USERADMIN_FIRSTNAME: Admin
      ZITADEL_FIRSTINSTANCE_USERADMIN_LASTNAME: openDesk
      ZITADEL_FIRSTINSTANCE_USERADMIN_PASSWORD: ${ZITADEL_ADMIN_PASSWORD:-CHANGEME_zitadel}
      # ── Instance ID (stable across restarts) ──
      ZITADEL_FIRSTINSTANCE_MACHINE_KEYPATH: /machinekey
      ZITADEL_FIRSTINSTANCE_MACHINE_KEYSIZE: 4096
      # ── Security ──
      ZITADEL_DEFAULTINSTANCE_PASSWORD_COMPLEXITY_POLICY: 0
    volumes:
      - ./idm/secrets:/secrets:ro
      - zitadel-machinekey:/machinekey
      # busybox (extracted by the deploy role) provides a wget for the
      # healthcheck — the distroless image has no shell/http client, and
      # `zitadel ready` insists on an https probe under --tlsMode external
      # (the server only speaks plain HTTP on :8080).
      - ./idm/zitadel/busybox:/usr/local/bin/busybox:ro
    healthcheck:
      test: ["CMD", "/usr/local/bin/busybox", "wget", "-q", "-O", "/dev/null", "http://localhost:8080/debug/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - opendesk-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zitadel.rule=Host(`${ZITADEL_DOMAIN:-auth.opendesk-sme.org}`)"
      - "traefik.http.routers.zitadel.tls=true"
      - "traefik.http.routers.zitadel.tls.certresolver=letsencrypt"
      - "traefik.http.routers.zitadel.entrypoints=websecure"
      - "traefik.http.services.zitadel.loadbalancer.server.port=8080"
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M

volumes:
  zitadel-machinekey:
