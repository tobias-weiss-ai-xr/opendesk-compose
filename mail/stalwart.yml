# ── Stalwart Mail Server overlay ───────────────
# Include via: COMPOSE_FILE=docker-compose.yml:mail/stalwart.yml
# Rust-based mail server: SMTP, IMAP, JMAP, CalDAV, CardDAV.
#
# Config is rendered from config.toml.template via envsubst at startup.

services:
  stalwart:
    image: stalwartlabs/stalwart:v0.16
    container_name: opendesk-stalwart
    hostname: mail.${OPENDESK_DOMAIN:-opendesk-sme.org}
    restart: unless-stopped
    ports:
      - "25:25"
      - "465:465"
      - "587:587"
      - "993:993"
    environment:
      TZ: Europe/Berlin
      MAIL_DOMAIN: ${MAIL_DOMAIN:-mail.opendesk-sme.org}
      STALWART_PUBLIC_URL: https://${MAIL_DOMAIN:-mail.opendesk-sme.org}
      STALWART_RECOVERY_ADMIN: admin:${STALWART_ADMIN_PASSWORD:-}
    volumes:
      - stalwart-etc:/etc/stalwart
      - stalwart-data:/var/lib/stalwart
      - ./mail/stalwart/config.toml.template:/etc/stalwart/config.toml.template:ro
      - ./mail/stalwart/stalwart-entrypoint.sh:/stalwart-entrypoint.sh:ro
    entrypoint: ["/stalwart-entrypoint.sh"]
    command: ["stalwart", "--config", "/etc/stalwart/config.toml"]
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/8080'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - opendesk-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.stalwart.rule=Host(`${MAIL_DOMAIN:-mail.opendesk-sme.org}`)"
      - "traefik.http.routers.stalwart.tls=true"
      - "traefik.http.routers.stalwart.tls.certresolver=letsencrypt"
      - "traefik.http.routers.stalwart.entrypoints=websecure"
      - "traefik.http.services.stalwart.loadbalancer.server.port=8080"
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G
        reservations:
          cpus: "0.5"
          memory: 512M

volumes:
  stalwart-etc:
  stalwart-data:
